<% @meta_title = "#{resource.name} artists profile on Sidestage.com" %>
<%= render 'profile' %>

<%= content_for :olark do %>
    <%= render "olark" %>
<% end %>

<%= render flash[:auto_modal] if flash[:auto_modal].present? %>