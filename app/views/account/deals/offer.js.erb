<% if resource.errors.present? %>
  <% #don't know yet what to do' %>
<% else %>
  vex.closeAll();
  $("[data-container='<%= dom_id(resource) %>']").html("<%= j render("#{resource.state}", deal: resource).strip.html_safe %>");
  vex.dialog.alert('<%= t(:"application.deals.update.success") %>');
<% end %>