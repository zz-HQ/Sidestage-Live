<% if resource.errors.present? %>
  vex.closeAll();
  vex.open({ content: "<%= j render("update_price", deal: resource).strip.html_safe %>" });
<% else %>
    window.location = "<%= account_conversation_path(resource.conversation) %>"
<% end %>