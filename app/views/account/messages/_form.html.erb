<div class="form-inputs">
	<%= f.input :body, autofocus: true, label: false %>
	<%= f.input :receiver_id, as: :hidden %>	
	<%= f.submit class: "btn btn-blue", data: { disable_with: "#{t(:"helpers.submit.message.disable_with")}" } %>
</div>
