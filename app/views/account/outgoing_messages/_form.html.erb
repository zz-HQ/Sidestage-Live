<h2>Reply Artist</h2>
<div class="form-inputs">
	<%= f.input :body, autofocus: true, placeholder: "Message", label: false %>
	<%= f.input :receiver_id, as: :hidden %>	
	<%= f.input :conversation_id, as: :hidden %>
	<%= f.button :submit, value: "Reply", class: "btn" %>
</div>
