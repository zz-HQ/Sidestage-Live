<div id="personals" class="container">
    
	<%= render "sidebar" %>

  <div class="content">
    <header>Password</header>
    <%= simple_form_for resource, url: password_account_personal_path do |f| %>
      <%= f.input :current_password, input_html: { autocomplete: "off" } %>
      <%= f.input :password, html_input: { autocomplete: "off" } %>
      <%= f.input :password_confirmation, html_input: { autocomplete: "off" } %>
      <div class="input">
      	<%= f.button :submit, value: "Update", class: "btn" %>
      </div>
    <% end %>
  </div>
</div>