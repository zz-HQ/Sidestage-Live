$("#<%= dom_id(resource) %>").remove();

if($("[data-container=pictures] > li").length == 0) {
	$("#fileupload-button").remove();
	$("[data-container=pictures]").html("<%= j render 'no_pictures' %>");
}

<% if @profile.present? %>
    <%= render "account/profiles/update" %>
<% end %>