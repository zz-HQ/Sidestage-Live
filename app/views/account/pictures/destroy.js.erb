$("#<%= dom_id(resource) %>").remove();

if($("#pictures > li").length == 0) {
	$("#fileupload-button").remove();
	$("#pictures").html("<%= j render 'no_pictures' %>");
}

<% if @profile.present? %>
    <%= render "account/profiles/update" %>
<% end %>