<h1>Dashboard</h1>

<h2>Conversations</h2>

<% current_user.conversations.each do |conversation| %>
<h4><%= link_to conversation.negotiator_for(current_user).name, account_conversation_path(conversation) %></h4>
<p>
	<%= conversation.body %>
</p>
<% end %>
