vex.close()
<% if resource.errors.present? %>
  vex.closeAll();
  vex.open({content: "<%=j render("form", deal: resource)).strip.html_safe %>"});
<% else %>
  $("#deal-teaser").html("<%=j render("/account/conversations/deal_teaser", deal: resource).strip.html_safe %>")
<% end %>  
