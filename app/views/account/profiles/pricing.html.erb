<div id="profile" class="container">
  <aside>
    <%= render 'sidebar' %>
  </aside>

  <div class="content pricing">
    <header><%= t(:"application.profiles.pricing.title_html") %></header>
    
    <div id="pricing">
      <%= simple_form_for resource, url: pricing_account_profile_path do |f| %>
        <%= render "pricing_form", f: f %>
      <% end %>
    </div>
  </div>
</div>      
