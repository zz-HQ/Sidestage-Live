<%= simple_form_for resource, url: description_account_profile_path, html: { id: :social_form }, remote: true, data: { auto_save: true } do |f| %>
    <div class="inputs profile_social_media">
    	<%= f.input :facebook %>

    	<div class="inputs twitter-field">
    		<%= f.input :twitter %>
    		<span>@</span>
    	</div>
    </div>
<% end %>
