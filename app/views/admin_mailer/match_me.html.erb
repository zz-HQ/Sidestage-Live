<% MatchMe::TRAVERSABLE_ATTRIBUTES.each do |attr| %>
    <p style="color: #4a4a4a; font-size: 18px; margin: 50px 4%; font-weight: 300; line-height: 1.45;"><%= MatchMe.human_attribute_name(attr) %>: <%= @match_me.send(attr) %></p>
<% end %>