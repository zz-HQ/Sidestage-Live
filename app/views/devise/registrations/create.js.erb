<% if resource.errors.present? %>
  $('.devise-lightbox').replaceWith("<%= j render("form").strip.html_safe %>");  
<% else %>
  window.location.href = "<%= after_sign_up_path_for(resource) %>";
<% end %>
